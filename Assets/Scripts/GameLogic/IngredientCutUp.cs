using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IngredientCutUp : MonoBehaviour
{
    public IngredientType InputIngredient;
    
    public List<IngredientType> OutputIngredientTypes = new List<IngredientType>();
    public List<int> OutputIngredientAmounts = new List<int>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
