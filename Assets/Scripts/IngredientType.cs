using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IngredientType : MonoBehaviour
{
    //public string id;
    public string ingredientName;
    public Color color;
    public Sprite inventorySprite;
    /*
    public class IngredientType
    {
        public string id;
        public string name;
        public int color_r;
        public int color_g;
        public int color_b;
        public Sprite inventorySprite;

        public IngredientType(string pId, string pName, string pColor_r, string pColor_g, string pColor_b, Sprite pInventorySprite)
        {
            id = pId;
            name = pName;
            color_r = pColor_r;
            color_g = pColor_g;
            color_b = pColor_b;
            inventorySprite = pInventorySprite;
        }

        public IngredientType(string pId)
        {
            {
                id = pId;
                name = "Placeholder Ingredient";
                color_r = 200;
                color_g = 100;
                color_b = 50;
                inventorySprite = pInventorySprite;
            }
        }
    } */

   /* private void InitializeIngredientTypes
    {

    } */

    // Start is called before the first frame update
    void Start()
    {
        //InitializeIngredientTypes();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
